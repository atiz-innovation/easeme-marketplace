```mermaid
flowchart TD
    A[เริ่มต้น] --> B[ผู้ใช้ไม่มีบัญชีและกรอกหมายเลขโทรศัพท์/อีเมล]
    B --> C[ผู้ใช้ขอ OTP]
    C --> D[ระบบสร้างและส่ง OTP ภายใน 5 วินาที]
    D --> E[ผู้ใช้กรอก OTP เพื่อยืนยัน]
    E --> F[ระบบตรวจสอบความถูกต้องของ OTP]
    F -->|OTP ถูกต้อง| G[ระบบสร้าง JWT Access + Refresh Token]
    F -->|OTP หมดอายุ (5 นาที) หรือไม่ถูกต้อง| H[แสดงข้อความผิดพลาดที่ชัดเจน]
    G --> I[เข้าสู่ระบบสำเร็จ / ไปหน้าจองบริการ]
    H --> I2[ผู้ใช้สามารถขอ OTP ใหม่ได้]

    %% Styling
    classDef process fill:#f9f9f9,stroke:#333,stroke-width:1px;
    class A,B,C,D,E,F,G,H,I,I2 process;

```
