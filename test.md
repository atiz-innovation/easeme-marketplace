```mermaid
flowchart TD
    A[ลูกค้าเริ่มลงทะเบียน / เข้าสู่ระบบ] --> B[กรอกหมายเลขโทรศัพท์หรืออีเมล]
    B --> C{มีบัญชีอยู่แล้วหรือไม่?}
    C -->|ไม่มี| D[ระบบขอ OTP สำหรับเบอร์หรืออีเมลนั้น]
    C -->|มีแล้ว| E[ดำเนินการเข้าสู่ระบบโดย OTP เช่นเดียวกัน]
    D --> F[ผู้ใช้กด "ขอ OTP"]
    F --> G[ระบบสร้างและส่ง OTP ภายใน 5 วินาที]
    G --> H[ผู้ใช้กรอก OTP ที่ได้รับ]
    H --> I{OTP ถูกต้องและยังไม่หมดอายุ (5 นาที)?}
    I -->|ใช่| J[ระบบยืนยันตัวตนสำเร็จ → ออก JWT access + refresh token]
    I -->|ไม่ใช่| K[แสดงข้อความผิดพลาดชัดเจน (OTP ผิดหรือหมดอายุ)]
    J --> L[ผู้ใช้เข้าสู่ระบบสำเร็จ / พร้อมเริ่มจองบริการ]


```
