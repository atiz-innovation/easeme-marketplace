# US026 - ดูประวัติการจอง (Booking History)

## 👤 บทบาท
- ลูกค้า

## 🎯 เป้าหมายของเคส
- ในฐานะ: ในฐานะลูกค้าที่เคยมีการจอง
- ต้องการ: ต้องการดูประวัติการจองทั้งหมด
- เพื่อ: เพื่อดูข้อมูลย้อนหลังและสถานะปัจจุบัน

## ⚙️ เงื่อนไขก่อนเริ่ม (Precondition)
- ลูกค้า logged in และมี booking เกิดขึ้นก่อนหน้า

## 🧭 ผลลัพธ์และสถานการณ์
- ✅ ผลลัพธ์ที่คาดหวัง (Success Flow): ระบบแสดงรายการ booking ทั้งหมด past/upcoming พร้อมสถานะและลิงก์รายละเอียด
- ❌ ผลลัพธ์ที่ Failure:  
  - ไม่มีประวัติการจอง: ระบบแสดงข้อความว่าไม่มีประวัติการจอง
  - ระบบล่ม/ตอบสนองช้า: ระบบแสดงข้อความข้อผิดพลาดและให้ลองใหม่
  - หน้าเพจไม่ถูกต้องหรือข้อมูลผิดพลาดบนหน้า: แจ้งผู้ใช้งานและให้ย้อนกลับ
  - ปัญหาการนำลิงก์ไปดูรายละเอียด: ลิงก์ไม่ทำงานหรือโหลดรายละเอียดไม่ได้
- 🔄 ผลลัพธ์ทางเลือก:  
  - รองรับการกรอง/เรียงลำดับในอนาคต (วันที่, สถานะ)
  - รองรับการค้นหาผู้ให้บริการหรือรายการที่มีอยู่
  - ใช้ตัวกรองเพิ่มเติม เช่น ระยะเวลาทั้งหมด หรือสถานะทั้งหมด
- ⚠️ ผลลัพธ์ขอบเขตพิเศษ:  
  - session หมดอายุ: ต้องนำผู้ใช้งานไปยังหน้าล็อกอิน
  - timeout ระหว่างดึงข้อมูล: แสดงข้อความ timeout และให้ลองใหม่
  - ระบบขัดข้องระหว่างทาง: แจ้งข้อผิดพลาดและให้ลองใหม่

## ✅ เกณฑ์การยอมรับ (Acceptance Criteria)
- แสดง booking_id, วันที่, ผู้ให้บริการ, สถานะ, ยอดเงิน
- มี Pagination
- response <2s typical

---

## 🔁 Mermaid Sequence Diagram  
> แสดงลำดับเหตุการณ์ระหว่าง "ผู้ใช้" กับ "ระบบ"

```mermaid
sequenceDiagram
  autonumber
  participant U as 🧑 ผู้ใช้
  participant S as 💻 ระบบ
  U-&gt;&gt;S: กดดูประวัติการจอง
  S--&gt;&gt;U: แสดงรายการ bookings พร้อมข้อมูล
```

---

## 🧭 Mermaid Flowchart TD  
> แสดงขั้นตอนการทำงานของระบบอย่างเข้าใจง่าย

```mermaid
flowchart TD
  A[เริ่มต้น] --&gt; B[ตรวจสอบ: เงื่อนไข]
  B --&gt; C[ดำเนินการ: เมื่อ]
  C --&gt; D[ผลลัพธ์สำเร็จ: ผลลัพธ์ที่คาดหวัง]
  C --&gt; E[ผลลัพธ์ล้มเหลว: ผลลัพธ์ที่Failure]
  C --&gt; F[ผลลัพธ์ทางเลือก: ผลลัพธ์ทางเลือก]
  C --&gt; G[ขอบเขตพิเศษ: ผลลัพธ์ขอบเขตพิเศษ]
  D --&gt; H[สิ้นสุด]
  E --&gt; H
  F --&gt; H
  G --&gt; H
```

